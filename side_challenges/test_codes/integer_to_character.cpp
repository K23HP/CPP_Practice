#include <iostream>

using namespace std;

// Function prototype

int main() {
    // // Integer modulo 27 indicates the letter Alphabet (where 1 = A and so on)
    // cout << int('A') << endl;
    // cout << char((143 % 27) + 64) << endl;

    // // The remainder of dividing the integer by 27 represents the lowercase letters (1 = a anf so on)
    // cout << int('a') << endl;
    // cout << char((55 % 27) + 96) << endl;
    
    // The remainder of dividing the integer by 27 represents the lowercase letters (1 = a anf so on)
    // cout << int('!') << endl;
    cout << char((19 % 9) + 32) << endl;

    return 0;
}


